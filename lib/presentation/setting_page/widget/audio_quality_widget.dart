import 'package:flutter/material.dart';

class AudioQualityWidget extends StatelessWidget {
  const AudioQualityWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Text('Audio Quality'),

      ],
    );
  }
}
