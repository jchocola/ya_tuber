import 'dart:ui';

import 'package:flutter/material.dart';

class DarkAppColor {
  /// Основной фон (тёмно-розово-фиолетовый)
  static const baseColor = Color(0xFFE9EEF6);

  /// Акцент (иконки, текст, слайдеры)
  static const accentColor =Color(0xFFF2F5FB);

  /// Вариант (поверхности, кнопки)
  static const variantColor = Color(0xFFE1E7F0);
}