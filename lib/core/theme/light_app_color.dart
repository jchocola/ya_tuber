import 'dart:ui';

import 'package:flutter/material.dart';

class LightAppColor {
  static const baseColor = Color(0xffeff0f1);
  static const accentColor = Colors.grey;
  static const variantColor = Colors.white70;
}
